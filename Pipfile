[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
mkdocs-material = "~=8.5.0"
mkdocs = "~=1.4.0"

[dev-packages]

[requires]
python_version = "3.9"

[scripts]
build = "mkdocs build"
start = "mkdocs serve"
serve = "mkdocs serve"
gh-deploy = "mkdocs gh-deploy --force"
